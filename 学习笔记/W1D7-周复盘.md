# W1D7 周复盘

## 一、本周完成度

如果只基于当前仓库里的可见成果，这一周已经完成了从聊天应用到 Embedding、再到 Function Calling 和 RAG 的一条连续学习链路。

完成情况可以这样判断：

- 概念学习：已完成
- 中文笔记沉淀：已完成
- 最小代码验证：已完成
- 工程理解串联：已完成基础版

整体完成度可以评估为：

**7.5 / 10**

原因：

- 已经有可运行结果，不再只是看课
- 已经开始用自己的代码去解释课程
- 但还没有把这些内容整合成一个统一的小项目

## 二、本周真正掌握的内容

### 已经比较稳的

- Chat 应用的基本请求链路
- Embedding 与语义搜索的核心概念
- Function Calling 的两阶段执行流程
- RAG 的最小闭环：文档 -> embedding -> 检索 -> 上下文增强 -> 回答

### 还不够稳的

- Notebook 与脚本之间的迁移能力
- 向量检索参数调优，例如 chunk 大小、Top K、阈值
- Function schema 设计的细节
- 如何把 RAG 和 Function Calling 放进同一个真实应用

## 三、本周最有价值的收获

### 1. 学习方式升级了

你这周不只是“看懂了”，而是开始做下面这件很关键的事：

- 读课程
- 抽核心概念
- 写中文笔记
- 补最小 demo
- 本地验证输出

这套方法非常值钱，因为它会让后面的学习越来越快。

### 2. 开始建立“模式识别”

你已经能开始识别几类常见 AI 应用模式：

- Chat 模式
- Search / Embedding 模式
- Tool Use / Function Calling 模式
- Grounded Answer / RAG 模式

这意味着你以后看新项目时，不再只是看代码，而会问：

- 它是在“生成”什么？
- 它在“检索”什么？
- 它有没有“调用工具”？
- 它的知识来自模型本身还是外部数据？

## 四、本周卡点

### 1. 环境与依赖

- Jupyter 服务会掉线，需要重启
- Notebook 中旧依赖和本机环境不完全一致
- API Key 读取方式和课程默认方式不一致，需要手动改造

### 2. 课程材料与实际可运行性之间有距离

- 一些 notebook 偏教学展示，不适合直接拿来稳定运行
- 不少示例依赖云端配置，必须额外做本地最小 demo 才更适合学习

### 3. 知识连接还在形成

现在你已经分别理解了：

- Embedding
- Function Calling
- RAG

但“它们在一个完整应用里如何协作”还需要下一周继续串起来。

## 五、下周最值得做的事

### 优先级 1

把 `Chat + RAG` 合成一个最小问答应用。

目标：

- 用户输入问题
- 先检索本地资料
- 再让模型回答

### 优先级 2

把 `Function Calling + RAG` 放到同一个流程里。

目标：

- 模型先判断是“查知识”还是“调工具”
- 如果是知识问题，走 RAG
- 如果是动作型问题，走 Function Calling

### 优先级 3

补一份“本周核心概念对照表”。

建议你能用自己的话区分：

- Prompt Engineering
- Embedding
- Function Calling
- RAG
- Fine-tuning

## 六、给自己的结论

这一周最值得肯定的地方，是你已经从“课程跟学”走到了“开始构建自己的学习产物”。

这非常重要，因为从这个阶段开始，学习的成果不再只存在于记忆里，而是已经留在：

- 文档里
- 脚本里
- demo 里
- 可复用的方法里

## 七、下周开始前的准备动作

下周开始前，只做三件小事：

1. 把本周 3 个 demo 都再跑一遍
2. 用自己的话讲一遍 Function Calling 和 RAG 的区别
3. 选一个你最想做的小应用方向，作为下周主线
